package pt.jorgeduarte.flink_consumer;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.data.jpa.repository.config.EnableJpaRepositories;

@SpringBootApplication
@EnableJpaRepositories(basePackages = "pt.jorgeduarte.flink_consumer.persistent.repositories")
public class FlinkConsumerApplication implements CommandLineRunner {
    private FlinkConsumer flinkConsumer;
    public FlinkConsumerApplication(FlinkConsumer flinkConsumer){
        this.flinkConsumer = flinkConsumer;
    }

    public static void main(String[] args) {
        SpringApplication.run(FlinkConsumerApplication.class, args);
    }

    @Override
    public void run(String... args) throws Exception {
        flinkConsumer.start();
    }
}
*/